# AGENTS.md

## Cursor Cloud specific instructions

This repository (`thomaspas/thoma`) is currently a **greenfield / placeholder** project. As of setup, it contains only `README.md` (`# thoma`) and no application source, package manifests, lockfiles, Docker configs, or CI.

- There are **no services** to start, and **no lint / test / build / run** commands yet.
- Do not invent a stack or scaffold an app unless the user explicitly asks for that.
- When application code is added, update this section with the real startup, lint, test, and gotchas for that stack.
